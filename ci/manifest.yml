applications:
- name: vmms-backend
  instances: 1
  memory: 1024M
  disk_quota: 1024M
  routes:
  - route: vmms-backend.cfapps.io
  stack: cflinuxfs2
  services:
  - postgresql-vmms-backend
  buildpack: java_buildpack
  env:
    SPRING_PROFILES_ACTIVE: prod